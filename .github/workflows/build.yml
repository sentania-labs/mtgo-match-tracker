name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-app:
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.owner.login == 'sentania-labs'
    runs-on: [self-hosted, linux, docker]
    permissions:
      contents: read
      packages: read
      security-events: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Multi-arch build (validate, no push)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          platforms: linux/amd64,linux/arm64
          push: false
          tags: tamiyo-app:ci
          cache-from: type=gha,scope=app
          cache-to: type=gha,mode=max,scope=app

      - name: Load amd64 image for tests
        if: github.event_name == 'pull_request'
        run: |
          docker build \
            --platform linux/amd64 \
            -t tamiyo-app:pr \
            -f Dockerfile \
            .

      - name: Trivy scan (CRITICAL/HIGH, fixed only)
        if: github.event_name == 'pull_request'
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: tamiyo-app:pr
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          exit-code: "1"
          format: table

      - name: In-container pytest
        if: github.event_name == 'pull_request'
        run: |
          # Use docker create + cp, NOT bind mounts — runner is DinD.
          CID=$(docker create \
            -e DATABASE_URL=sqlite+aiosqlite:///:memory: \
            tamiyo-app:pr \
            sh -c "pip install --no-cache-dir pytest pytest-asyncio pytest-cov httpx aiosqlite && pytest tests/ -v --cov=app --cov-fail-under=70")
          docker cp tests/. "$CID:/app/tests/"
          docker cp pyproject.toml "$CID:/app/pyproject.toml" 2>/dev/null || true
          docker start -a "$CID"
          RC=$?
          docker rm "$CID" >/dev/null
          exit $RC

      - name: Cleanup PR image
        if: always() && github.event_name == 'pull_request'
        run: docker rmi tamiyo-app:pr || true

  build-caddy:
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.owner.login == 'sentania-labs'
    runs-on: [self-hosted, linux, docker]
    permissions:
      contents: read
      packages: read
    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Multi-arch build (validate, no push)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: services/caddy/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: false
          tags: tamiyo-caddy:ci
          cache-from: type=gha,scope=caddy
          cache-to: type=gha,mode=max,scope=caddy

  compose-smoke-test:
    needs: [build-app, build-caddy]
    if: >-
      (github.event_name != 'pull_request' || github.event.pull_request.head.repo.owner.login == 'sentania-labs')
      && github.event_name == 'pull_request'
    runs-on: [self-hosted, linux, docker]
    steps:
      - uses: actions/checkout@v4

      - name: Build app image (smoke tag)
        run: docker build --platform linux/amd64 -t tamiyo-app:smoke -f Dockerfile .

      - name: Build caddy image (smoke tag)
        run: docker build --platform linux/amd64 -t tamiyo-caddy:smoke -f services/caddy/Dockerfile services/caddy

      - name: Seed .env
        run: |
          cat > .env <<'EOF'
          TLS_MODE=off
          DATABASE_URL=postgresql+asyncpg://mtgo:test@db/mtgo_tracker
          DB_PASSWORD=test
          SECRET_KEY=test-secret-key-32chars-minimum
          EOF

      - name: Bring up stack
        run: |
          export TAMIYO_APP_IMAGE=tamiyo-app:smoke
          export TAMIYO_CADDY_IMAGE=tamiyo-caddy:smoke
          docker compose up -d

      - name: Poll /healthz
        run: |
          for i in $(seq 1 30); do
            if docker compose exec -T app curl -fsS http://localhost:8000/healthz > /dev/null; then
              echo "healthz ok after ${i} polls"
              exit 0
            fi
            sleep 2
          done
          echo "healthz never returned 200 within 60s"
          docker compose logs app
          exit 1

      - name: Tear down
        if: always()
        run: docker compose down -v

      - name: Cleanup smoke images
        if: always()
        run: docker rmi tamiyo-app:smoke tamiyo-caddy:smoke || true
